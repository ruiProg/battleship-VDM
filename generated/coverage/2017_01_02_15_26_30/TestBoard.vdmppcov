class TestBoard
operations
	
	private testCreateBoard : () ==> ()
		testCreateBoard() == (
			dcl b: Board := new Board();
			TestBase`assertEqual(b.countCellType(<Empty>),100);
	);
	
	public run: () ==> ()
		run() == (
			testCreateBoard();
	);
end TestBoard